{% set name = "decaylanguage" %}
{% set version = "0.13.0" %}


package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/DecayLanguage-{{ version }}.tar.gz
  sha256: eaa68b796f01475186227a353fa94099da5a757d188f855e7cf0f249bcb82c6d

build:
  number: 0
  noarch: python
  script: {{ PYTHON }} -m pip install . -vv

requirements:
  host:
    - pip
    - python >=3.6
    - setuptools-scm
  run:
    - attrs >=19.2
    - deprecated
    - python-graphviz
    - lark-parser >=0.11.0
    - numpy >=1.12
    - pandas >=0.22
    - particle ==0.16.*
    - plumbum >=1.6.9
    - importlib-resources >=2.0
    - python >=3.6

test:
  imports:
    - decaylanguage
    - decaylanguage.data
  commands:
    - pip check
  requires:
    - pip

about:
  home: https://github.com/scikit-hep/decaylanguage
  summary: A language to describe particle decays, and tools to work with them.
  license: BSD-3-Clause
  license_file: LICENSE

extra:
  recipe-maintainers:
    - chrisburr
    - eduardo-rodrigues
